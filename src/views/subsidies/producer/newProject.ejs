<!DOCTYPE html>
<html lang="en" x-data="{ float: 0 }" @scroll.window="float = window.scrollY">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Green Hydrogen Project Submission</title>
    <script src="https://cdn.jsdelivr.net/npm/alpinejs@3.12.0/dist/cdn.min.js" defer></script>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@3.3.2/dist/tailwind.min.css" rel="stylesheet">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap');
        body { 
            background: linear-gradient(135deg, #00a67e, #006d5b); 
            min-height: 100vh; 
            font-family: 'Poppins', sans-serif; 
            color: #fff; 
            overflow-x: hidden;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 3rem 1rem;
        }
        .floating-energy { 
            position: absolute; 
            width: clamp(2rem, 5vw, 4rem);
            height: clamp(2rem, 5vw, 4rem);
            background: radial-gradient(circle, #00ff99 40%, #009966 100%); 
            border-radius: 50%; 
            opacity: 0.6; 
            animation: floatEnergy 8s infinite ease-in-out; 
            filter: blur(2px);
        }
        @keyframes floatEnergy { 
            0% { transform: translateY(0) rotate(0deg) scale(1); } 
            50% { transform: translateY(clamp(-1rem, -3vw, -2rem)) rotate(180deg) scale(1.1); } 
            100% { transform: translateY(0) rotate(360deg) scale(1); } 
        }
        form { 
            background: rgba(255,255,255,0.08); 
            backdrop-filter: blur(10px); 
            border-radius: 1.5rem; 
            padding: 2rem; 
            max-width: 700px; 
            width: 100%; 
            margin: 2rem auto; 
            border: 2px solid rgba(0,255,153,0.3);
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
            transition: all 0.5s ease;
        }
        @media (max-width: 768px) {
            h1 { font-size: 2.5rem; }
            form { padding: 1.5rem; }
        }
        input, textarea { 
            background: rgba(255,255,255,0.1); 
            border: 2px solid rgba(0,255,153,0.3); 
            border-radius: 0.75rem; 
            padding: 0.75rem 1rem; 
            color: #00ff99; 
            font-weight: 600; 
            width: 100%; 
            margin-bottom: 1rem; 
            transition: all 0.3s ease-in-out; 
        }
        input::placeholder, textarea::placeholder { color: rgba(0,255,153,0.5); font-weight: 400; }
        input:focus, textarea:focus { 
            outline: none; 
            border-color: #00ff99; 
            box-shadow: 0 0 10px #00ff99, 0 0 20px #00cc77; 
            background: rgba(255,255,255,0.2); 
            color: #00ff99;
            transform: scale(1.02);
        }
        button { 
            background: linear-gradient(135deg,#00ff99,#00cc77); 
            color: #004d33; 
            font-weight: bold; 
            padding: 0.75rem 1.5rem; 
            border-radius: 1rem; 
            border: none; 
            cursor: pointer; 
            font-size: 1.1rem; 
            transition: all 0.4s cubic-bezier(0.68, -0.55, 0.27, 1.55); 
            width: 100%; 
            text-shadow: 1px 1px 2px rgba(0,0,0,0.2);
        }
        button:hover { 
            transform: translateY(-5px) scale(1.02); 
            box-shadow: 0 15px 35px rgba(0,255,153,0.5); 
            background: linear-gradient(135deg,#00cc77,#00ff99); 
        }
        label { 
            font-weight: 600; 
            color: #00ff99; 
            display: block;
            margin-bottom: 0.5rem;
            transition: color 0.3s ease;
        }
        .form-group { position: relative; }
        .form-group:focus-within label { color: #fff; text-shadow: 0 0 5px #00ff99; }
    </style>
</head>
<body class="relative flex flex-col items-center min-h-screen py-12">
    <div class="floating-energy" style="top:15%; left:5%; animation-delay:0s; animation-duration: 8s;"></div>
    <div class="floating-energy" style="top:35%; left:75%; animation-delay:2s; animation-duration: 10s;"></div>
    <div class="floating-energy" style="top:60%; left:25%; animation-delay:4s; animation-duration: 9s;"></div>
    <div class="floating-energy" style="top:85%; left:60%; animation-delay:6s; animation-duration: 7s;"></div>

    <h1 class="text-4xl md:text-5xl font-bold text-green-200 mb-8 text-center px-4">
        🌱 Green Hydrogen Project Submission
    </h1>

    <% if (locals.message) { %>
        <div class="mb-6 bg-green-900 bg-opacity-70 text-green-100 p-4 rounded shadow-lg text-center mx-4">
            <%= locals.message %>
        </div>
    <% } %>

    <form action="/producer/Projects-save" method="POST">
        <div class="form-group">
            <label for="programId">Program ID</label>
            <input type="text" id="programId" name="programId" placeholder="e.g., GH-001" required>
        </div>
        <div class="form-group">
            <label for="name">Project Name</label>
            <input type="text" id="name" name="name" placeholder="e.g., Solar-Powered Electrolyzer" required>
        </div>
        <div class="form-group">
            <label for="milestone">Milestone Achieved</label>
            <input type="number" id="milestone" name="milestone" placeholder="e.g., 3 (for Milestone 3)" required>
        </div>
        <div class="form-group">
            <label for="metrics">Metrics / Notes</label>
            <textarea id="metrics" name="metrics" placeholder="e.g., 500 kg of H2 produced this month"></textarea>
        </div>
        <div class="form-group">
            <label for="email">Producer Email ID</label>
            <input type="email" id="email" name="email" placeholder="e.g., producer@example.com" required>
        </div>
        <button type="submit">Submit Project 🌿</button>
    </form>
</body>
</html>