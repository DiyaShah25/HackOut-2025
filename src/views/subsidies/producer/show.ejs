<!DOCTYPE html>
<html>
<head>
  <title><%= project.name %></title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 p-6">
  <h1 class="text-2xl font-bold mb-4"><%= project.name %></h1>
  <p><strong>Company:</strong> <%= project.company %></p>
  <p><strong>Location:</strong> <%= project.location %></p>
  <p><strong>Capacity:</strong> <%= project.capacity %> tons/year</p>
  <p><strong>Status:</strong> <%= project.status %></p>
  <p><strong>Description:</strong> <%= project.description %></p>

  <h2 class="mt-4 text-xl font-semibold">Milestones</h2>
  <ul>
    <% project.milestones.forEach(m => { %>
      <li>📝 <%= m.title %> - <%= m.achieved ? "✅ Achieved" : "⏳ Pending" %> - Target Date: <%= m.targetDate ? m.targetDate.toDateString() : "N/A" %></li>
    <% }) %>
  </ul>

  <h3 class="mt-4">Add Milestone</h3>
  <form action="/projects/<%= project._id %>/milestones" method="POST" class="mb-4">
    <input type="text" name="title" placeholder="Milestone Title" class="p-2 border rounded mb-2 w-full" required>
    <input type="text" name="description" placeholder="Description" class="p-2 border rounded mb-2 w-full">
    <input type="date" name="targetDate" class="p-2 border rounded mb-2 w-full">
    <button type="submit" class="bg-blue-600 text-white px-4 py-2 rounded">Add</button>
  </form>

  <form action="/projects/<%= project._id %>/delete" method="POST">
    <button type="submit" class="bg-red-600 text-white px-4 py-2 rounded">Delete Project</button>
  </form>
</body>
</html>
