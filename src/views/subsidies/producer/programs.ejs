<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Available Government Programs</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 p-6">
  <h1 class="text-3xl font-bold mb-6">Available Government Programs</h1>

  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
    <% programs.forEach(program => { %>
      <div class="bg-white p-5 rounded-lg shadow hover:shadow-lg transition">
        <h2 class="text-xl font-semibold mb-2"><%= program.name %></h2>
        
        <p><strong>ID: </strong> <%= program._id %></p>
        <p><strong>Milestone:</strong> <%= program.milestone %></p>
        <p><strong>Subsidy Amount:</strong> ₹<%= program.subsidyAmount %></p>
        <p><strong>Status:</strong> <%= program.status %></p>
        </div>
    <% }) %>
  </div>
</body>
</html>
