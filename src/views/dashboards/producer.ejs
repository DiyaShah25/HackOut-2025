<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Producer Dashboard</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      background: linear-gradient(135deg,#00a67e,#006d5b);
    }

    /* Card hover animation */
    .card {
      background: rgba(255,255,255,0.95);
      padding: 1.5rem;
      border-radius: 12px;
      box-shadow: 0 4px 15px rgba(0,0,0,0.1);
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .card:hover {
      transform: translateY(-6px);
      box-shadow: 0 12px 24px rgba(0,0,0,0.2);
    }

    /* Button animation */
    .btn {
      background: linear-gradient(135deg,#00a67e,#008e6c);
      color: #fff;
      padding: 0.7rem 1.5rem;
      border-radius: 8px;
      font-weight: bold;
      cursor: pointer;
      transition: transform 0.3s ease, background 0.3s ease;
    }

    .btn:hover {
      transform: scale(1.05);
      background: linear-gradient(135deg,#008e6c,#00a67e);
    }

    /* Smooth fade-in animation for cards */
    .fade-in {
      opacity: 0;
      transform: translateY(20px);
      animation: fadeInUp 0.6s forwards;
    }

    @keyframes fadeInUp {
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
  </style>
</head>
<body class="min-h-screen flex flex-col">

  <!-- Header -->
  <header class="bg-white bg-opacity-90 p-4 shadow-md flex flex-col md:flex-row justify-between items-center gap-2 md:gap-0">
    <h1 class="text-green-700 font-bold text-2xl md:text-3xl">🌱 Producer Dashboard</h1>
    <p class="text-green-800 font-semibold">Welcome, <%= user.name %></p>
    <button class="btn"><a href="/">LogOut</a></button>
  </header>

  <!-- Container -->
  <main class="flex-1 p-6 grid gap-6 sm:grid-cols-1 md:grid-cols-2 lg:grid-cols-3">

    <!-- View Subsidy Schemes -->
    <div class="card fade-in text-center">
      <h2 class="font-bold text-green-700 text-lg mb-4">View Subsidy Schemes</h2>
      <a href="/producer/programs" class="btn inline-block">View Schemes</a>
    </div>

    <!-- Apply for Scheme -->
    <div class="card fade-in">
      <h2 class="font-bold text-green-700 text-lg mb-4 text-center">Apply for Scheme</h2>
      <form action="/producer/subsidies/projects" method="post" class="flex flex-col gap-3">
        <input type="text" name="programId" placeholder="Program ID" required class="p-2 border rounded focus:outline-none focus:border-green-600 focus:ring-1 focus:ring-green-400">
        <input type="text" name="name" placeholder="Program Name" required class="p-2 border rounded focus:outline-none focus:border-green-600 focus:ring-1 focus:ring-green-400">
        <button type="submit" class="btn mt-2">Apply</button>
      </form>
    </div>


    <!-- View Submitted Projects -->
    <div class="card fade-in text-center">
      <h2 class="font-bold text-green-700 text-lg mb-4">Submitted Projects</h2>
      <a href="/producer/viewProjects" class="btn">View Projects</a>
    </div>

  </main>

</body>
</html>
